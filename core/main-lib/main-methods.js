var validation = {}

validation.isValidString = (string, message = 'Special characters are not allowed', not_allowed = /([*+?^${}><\[\]\/\\])/g) => {
    var response = true;
    if (value.match(arg)) { response = false; }

    return response;
}
